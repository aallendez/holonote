repos:
  # ================================
  # 1. GENERAL SAFETY HOOKS
  # ================================
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: end-of-file-fixer
      - id: trailing-whitespace

  # ================================
  # 2. SECRET SCANNING
  # ================================
  - repo: local
    hooks:
      - id: gitleaks
        name: detect-secrets
        entry: ./.gitleaks-hook.sh
        language: system
        pass_filenames: false
        always_run: true

  # ================================
  # 3. TERRAFORM HOOKS (infra/)
  # ================================
  - repo: https://github.com/antonbabenko/pre-commit-terraform
    rev: v1.92.0
    hooks:
      - id: terraform_fmt
        files: ^infra/
      - id: terraform_validate
        files: ^infra/

  # ================================
  # 4. BACKEND PYTHON HOOKS (backend/)
  # ================================
  - repo: https://github.com/psf/black
    rev: 24.1.0
    hooks:
      - id: black
        language_version: python3.11
        files: ^backend/

  - repo: https://github.com/PyCQA/isort
    rev: 5.13.0
    hooks:
      - id: isort
        language_version: python3.11
        files: ^backend/

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.3.0
    hooks:
      - id: ruff
        files: ^backend/

  - repo: https://github.com/PyCQA/bandit
    rev: 1.7.7
    hooks:
      - id: bandit
        args: ["-r", "backend/src"]
        files: ^backend/

  # ================================
  # 5. FRONTEND HOOKS (frontend/)
  # ================================
  - repo: local
    hooks:
      - id: frontend-prettier
        name: Frontend Prettier
        entry: npx prettier --check .
        language: system
        types_or: [javascript, css, json, yaml]
        files: ^frontend/

      - id: frontend-eslint
        name: Frontend ESLint
        entry: npx eslint .
        language: system
        types_or: [javascript]
        files: ^frontend/

      - id: frontend-typecheck
        name: TypeScript Type Check
        entry: npx tsc --noEmit
        language: system
        files: ^frontend/

  # ================================
  # 6. CUSTOM HOOK: Backend unit test quick run
  # Only runs tests in backend/ when Python files change
  # ================================
  - repo: local
    hooks:
      - id: backend-tests
        name: Backend Unit Tests (fast)
        entry: bash -c "cd backend && pytest -q --disable-warnings || exit 1"
        language: system
        files: ^backend/.*\.py$
